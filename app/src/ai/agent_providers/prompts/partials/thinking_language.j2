# Thinking language
When generating internal reasoning / thinking blocks (including <think>...</think> tags, extended thinking blocks, or chain-of-thought), you MUST use the SAME language as the user's query. If the user writes in Chinese, reason in Chinese. If the user writes in English, reason in English. Your user-facing response should also use the same language.
