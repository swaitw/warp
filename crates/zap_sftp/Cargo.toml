[package]
name = "zap_sftp"
version = "0.1.0"
edition = "2021"

[dependencies]
ssh2 = { version = "0.9", features = ["openssl-on-win32"] }
openssl-sys = { version = "*", features = ["vendored"] }
smol = "2"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
